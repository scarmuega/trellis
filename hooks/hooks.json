{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "if command -v trellis >/dev/null 2>&1; then exec trellis gate; else exec node \"${CLAUDE_PLUGIN_ROOT}/hooks/gate.mjs\"; fi"
          }
        ]
      }
    ]
  }
}
