---
provenance: authored
owner: org/founder
tags: []
---
# Domain

Instance prose the spec does not fix; `trellis.toml` carries the machine-read
config and the spec pin.

## Runtime binding

- Contract: the runtime companion of the spec version pinned in
  `trellis.toml`; adoption recorded in `decisions/0000-adopt-trellis.md`.
- Harness: Claude Code with the `trellis` plugin; interactive sessions at the root.
- Plan effectiveness: `/trellis:focus [scope]`; weekly `focus` ritual through `org/focus`.
- Escalation channel: none in this fixture — list every finding fully in the run report.
